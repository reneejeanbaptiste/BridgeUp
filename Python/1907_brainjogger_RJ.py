def challenge(x):
	for x in range(x+3, x+21,3):
		print(x)
challenge(int(raw_input('Type integer')))

fav_places={"Bronx":["ZOO","Garden"], "Rockaway":["My House","Beach"]}	
	for place in fav_places:
		sentence= "I like "
	sentence = "I like "
	places = fave_places[places]
	for place in places: 
		if len(places) ==1:
			sentence+=place + "in "+place + "."
		elif(len(places)) 


